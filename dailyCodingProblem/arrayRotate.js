// This problem was asked by Amazon.

// Given an array and a number k that's smaller than the length of the array, 
// rotate the array to the right k elements in-place.

function arrayRotate(arr){

    //plan
    //  take an O(1) k in array, split with array method
    //  join  end + start portions of the array

}

console.log(arrayRotate([1,2,3,4,5,6,7,8], 4))